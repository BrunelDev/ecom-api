version: "2"
sql:
  - engine: "postgresql"
    queries: "./internal/queries"
    schema: "./internal/schema"
    gen:
      go:
        package: "repo"
        out: "./internal/adapter/postgresql"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: true
